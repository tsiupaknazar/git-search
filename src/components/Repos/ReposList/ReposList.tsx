import styles from './ReposList.module.scss';

interface ReposListProps { }

export const ReposList = ({ }: ReposListProps) => (
  <div className={styles.reposList}>
    ReposList Component
  </div>
);
